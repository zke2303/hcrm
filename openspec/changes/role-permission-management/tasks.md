## 1. 数据库模型及基础层准备

- [x] 1.1 扩展 `SysRole` 模型：增加 `code`, `data_scope` (int) 字段
- [x] 1.2 创建 `SysMenu` 模型：包含菜单树属性、权限标识 `perms`、以及 `api_path` 字段
- [x] 1.3 创建 `SysRoleMenu` 关联表模型：实现角色与菜单（权限）的多对多关系
- [x] 1.4 创建 `SysRoleDept` 关联表模型：针对自定义数据范围的科室授权
- [x] 1.5 执行数据库迁移并初始化基础角色数据（超级管理员等）

## 2. 后端核心逻辑开发

- [x] 2.1 实现角色管理 CRUD API：支持增删改查、状态切换及角色复制逻辑
- [x] 2.2 实现菜单管理 API：获取完整菜单树、菜单增删改功能
- [x] 2.3 实现权限核心接口：根据角色 ID 获取/更新关联的菜单权限树
- [x] 2.4 实现用户授权接口：批量分配用户角色，并支持根据角色查用户列表
- [x] 2.5 封装鉴权中间件：实现 API 路径拦截及基于 Redis 的权限缓存逻辑
- [x] 2.6 实现数据范围拦截：拦截 SQL 并根据 `data_scope` 动态注入过滤条件。

## 3. 前端界面实现

- [ ] 3.1 搭建角色管理页面：左侧角色列表，右侧 Tab 页签（基本信息/菜单权限/数据范围/用户列表）
- [ ] 3.2 实现树形权限配置组件：支持父子联动勾选、半选。
- [ ] 3.3 实现用户选择与分配组件：角色的“用户列表”页签下，支持从系统用户库增删关联用户。
- [ ] 3.4 实现前端权限指令 `v-hasPerm` 及 `checkPerm` 工具函数。
- [ ] 3.5 联调角色复制、状态切换等快速操作逻辑。

## 4. 安全保护与审计日志

- [ ] 4.1 在 Service 层增加超级管理员（ID=1）的特殊保护逻辑（禁删、禁注销）
- [ ] 4.2 定义权限操作日志拦截：对敏感的权限变更请求进行自动审计记录
- [ ] 4.3 整体权限链路冒烟测试：模拟不同角色登录，验证菜单可见性及 API 响应状态。
