## 1. 后端 API 开发

- [x] 1.1 (P0): 定义用户管理相关的 DTO (Create/Update/List Request) 和 VO (User Response)。
- [ ] 1.2 (P0): 在 `Repository` 层实现用户基础信息的 CRUD、分页查询及状态切换逻辑，确保适配软删除。
- [ ] 1.3 (P0): 在 `Service` 层实现用户管理的业务逻辑，包含密码 Bcrypt 哈希处理和事务管理（关联更新 `users` 与 `user_roles`）。
- [ ] 1.4 (P1): 实现 `Handler` 层的方法，处理请求绑定、响应封装及管理员权限校验逻辑。
- [ ] 1.5 (P1): 在 `internal/app/app.go` 中注册用户管理模块的路由，并确保挂载了 `Auth()` 中间件。

## 2. 前端 UI 开发

- [ ] 2.1 (P1): 在 `src/features/sys-user` 下定义用户管理相关的 API 调用函数和 TypeScript 接口模型。
- [ ] 2.2 (P1): 实现用户列表主页面，集成分页、模糊搜索、科室筛选及状态快速切换按钮。
- [ ] 2.3 (P1): 实现新增/编辑用户的对话框组件，支持表单校验及提交。
- [ ] 2.4 (P2): 开发角色分配（授权）功能，通过多选框或穿梭框管理用户角色关联。
- [ ] 2.5 (P2): 实现管理员重置用户密码的前端交互模块。

## 3. 测试与验证

- [ ] 3.1 (P0): 为 `Service` 层编写单元测试，覆盖用户创建、科室过滤及重复账号校验等核心逻辑。
- [ ] 3.2 (P2): 执行前后端联调测试，并通过 Agent Browser 验证用户管理全流程的正确性。
