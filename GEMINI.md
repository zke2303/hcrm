# AGENTS.md

> 本文件定义 AI 在本项目中的**强制执行规范**。
> 任何 AI 生成的内容若违反本规范，视为不合格输出。

---

# 一、语言规范（强制）

1. 所有说明性文本必须使用 **中文**。
2. 代码注释必须使用 **英文**。
3. Git Commit Message 必须使用 **中文**。
4. 不允许中英混杂描述业务逻辑。

---

# 二、技术规范（强制）

1. 严格使用项目既定技术栈，不得擅自引入新框架。
2. 不允许新增未讨论的第三方依赖。
3. 所有新增依赖必须说明用途。
4. 不允许修改未在任务范围内的文件。

---

# 三、代码规范（强制）

1. 必须遵循项目现有代码风格。
2. 禁止生成示例代码替代真实实现。
3. 禁止伪代码。
4. 必须保证代码可运行，不允许省略关键实现。
5. 不允许输出与任务无关的解释性内容。
6. 数据库层面不允许使用外键和唯一索引，所有约束必须在业务层实现。
7. 对必要的字段添加索引，以提高查询性能。索引命名以“前缀*表名缩写/全名*字段名”格式命名，使用全小写并以下划线\_分隔。

---

# 四、任务执行规范（强制）

1. 一次只实现一个 Task。
2. 每完成一个 Task 必须明确标记完成状态。
3. 禁止自动扩展需求。
4. 若需求不明确，必须停止并请求澄清。

---

# 五、安全与边界（强制）

1. 不允许删除数据。
2. 不允许修改数据库结构（除非任务明确要求）。
3. 不允许输出敏感信息。
4. 不允许生成测试数据污染生产逻辑。

---

# 六、输出格式要求（强制）

1. 输出必须结构清晰。
2. 代码块必须使用正确语言标记。
3. 不允许输出多版本方案，除非明确要求。
4. 不允许加入表情符号。

---

# 违规处理

若 AI 输出违反以上任一条款，应立即停止当前输出，并重新生成符合规范的内容。
